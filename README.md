# SIH 2026

SIH 2026, update file accordingly.Check for the Index.js File which is the server for calling Groq API.
you have to create a .env  file with name of GROQ_API_KEY={example_key}.
then run the Index.js file with npm run start.
